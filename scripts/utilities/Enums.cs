public static class Enums
{
    public enum FaceDirection { FRONT, BACK, LEFT, RIGHT };

    public enum State { NONE, IDLE, WALK, CHOPPING, TILLING, WATERING };

    public enum Tools { NONE, AXE, TILL, WATER, CORN, TOMATO };
}
